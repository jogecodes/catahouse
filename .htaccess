# SPA fallback using ErrorDocument (no mod_rewrite needed)
DirectoryIndex index.html

# Redirect known app routes to index.html (helps some shared hosts)
RedirectMatch 302 ^/(rate|synthesis)$ /index.html

# As a universal fallback: when a 404 happens, serve index.html
ErrorDocument 404 /index.html 